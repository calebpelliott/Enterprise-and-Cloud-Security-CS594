CREATE TABLE USERS (USERNAME VARCHAR(20) NOT NULL, NAME VARCHAR(255), OTPSECRET VARCHAR(255), PASSWORD VARCHAR(1600) NOT NULL, PRIMARY KEY (USERNAME))
CREATE TABLE ROLES (ROLENAME VARCHAR(20) NOT NULL, DESCRIPTION VARCHAR(20), PRIMARY KEY (ROLENAME))
CREATE TABLE MESSAGE (ID BIGINT NOT NULL, SENDER VARCHAR(20), TEXT VARCHAR(60), TIMESTAMP DATE, PRIMARY KEY (ID))
CREATE TABLE USERS_ROLES (USERNAME VARCHAR(20) NOT NULL, ROLENAME VARCHAR(20) NOT NULL, PRIMARY KEY (USERNAME, ROLENAME))
ALTER TABLE USERS_ROLES ADD CONSTRAINT FK_USERS_ROLES_USERNAME FOREIGN KEY (USERNAME) REFERENCES USERS (USERNAME)
ALTER TABLE USERS_ROLES ADD CONSTRAINT FK_USERS_ROLES_ROLENAME FOREIGN KEY (ROLENAME) REFERENCES ROLES (ROLENAME)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
